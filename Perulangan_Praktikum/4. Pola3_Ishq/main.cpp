#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
    int a,b;
    for (a=1; a<=10; a++)
        if (a*10);
        {
            for (b=1; b<=a; b++)
            cout<<setw(3)<<a;
            cout<<"\n";
        }
}
